version: 2

schema: raw
models:

  - name: country_raw
    description: test staging table
    columns:
      - name: nation_key
        tests:
#          - unique
#          - not_null
      - name: name  